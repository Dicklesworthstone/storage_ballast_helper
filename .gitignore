# Rust build artifacts
/target/

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db
._*

# Local environment / secrets
.env
.env.*
!.env.example

# Local runtime artifacts
*.log
*.pid
*.tmp
*.temp
.sbh/
var/
run/
tmp/

# Local planning state and caches
.bv/

# Ephemeral agent/runtime artifacts
storage.sqlite3
storage.sqlite3-*
rustc-ice-*.txt
a.out
perf.data
perf.data.*
--graph-format.svg
target-codex*/
target_*/
target-local/
/AUDIT_FINDINGS.md
/REVIEW_FINDINGS.md
/SESSION_REPORT.md
/UPGRADE_LOG.md

# Ephemeral files (agent-generated, build artifacts, test detritus)
ci-artifacts/
proptest-regressions/
*.proptest-regressions
test_meminfo

# Accidental shell output
/test
test_cast
agy.mcp.json
codex.mcp.json
cursor.mcp.json
factory.mcp.json
gemini.mcp.json
windsurf.mcp.json

# === multi-repo ephemeral cleanup 2026-08-03 ===
# Local Codex CLI project config (machine-specific; never share).
.codex/

# === multi-repo ephemeral patterns 2026-08-15 ===
# br fsqlite-ns VFS sidecars written beside the local beads DB
.beads/*-fsqlite-ns-gate
.beads/*-fsqlite-ns-use
.beads/*.db-wal-cert
.beads/*.db-wal-cert-head
